# .env.local - Local environment overrides
# This file overrides .env for local development
# Copy this to .env.local and update with your IP address

# Use your computer's local IP address for multi-device testing
# Get your IP:
#   Windows: ipconfig
#   Mac/Linux: ifconfig
# Look for IPv4 Address (usually starts with 192.168.x.x or 10.x.x.x)

VITE_API_URL=http://192.168.x.x:5000
